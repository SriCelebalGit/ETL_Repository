[pytest]
testpaths = tests
pythonpath = src notebooks
addopts = -q --strict-markers
filterwarnings =
    # Spark's py4j and Arrow emit deprecation noise that drowns real warnings.
    ignore::DeprecationWarning:pyspark.*
